export default function NewCardPage() {
  return (
    <main className="page">
      <section className="intro" aria-labelledby="new-card-title">
        <p className="eyebrow">New card</p>
        <h1 id="new-card-title">Add a card</h1>
        <p className="lede">
          Create the question, answer, and category in one focused view.
        </p>
      </section>
    </main>
  );
}
